# 文档归档说明

此目录用于存放已经不应作为当前开发依据的历史文档、证据快照存根和中间产物说明。

## 当前归档内容

- `showcase-page.md`：原展示页快照存根，真实内容已迁移到 `.sisyphus/evidence/`
- `timeline-page.md`：原时间线页面快照存根，真实内容已迁移到 `.sisyphus/evidence/`
- `r2-files-api.md`：原 R2 API 响应快照存根，真实内容已迁移到 `.sisyphus/evidence/`

## 使用规则

- 此目录中的文档默认不应作为当前实现规范或开发指令。
- 需要真实实现信息时，优先查看代码、配置文件、根目录 README 与各级 `AGENTS.md`。
- 若某份归档文档重新变为活跃文档，应移动回合适位置并同步更新说明。
